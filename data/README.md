# InsertAny3D 实验数据

本目录保存 InsertAny3D 的实验输入、渲染数据和实验输出，不包含模型权重或虚拟环境。

`third_party/MVInpainter/data/mvimagenet.tar` 和 `masks.tar` 是为了绕过托管平台的小文件数量限制而生成的完整归档。下载后可在其所在目录执行：

```bash
tar -xf mvimagenet.tar
tar -xf masks.tar
```
